<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SicilyLinesMobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SicilyLinesMobile"
    Title="SicilyLinesMobile">

    <ShellContent
        Title=""
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent Title="EditProfile"
              ContentTemplate="{DataTemplate local:EditProfile}"
              Route="EditProfile" />
    <ShellContent Title="EditProfile"
              ContentTemplate="{DataTemplate local:DashboardPage}"
              Route="DashboardPage" />
    <ShellContent Title="Réservations"
              Route="ReservationsPage"
              ContentTemplate="{DataTemplate local:ReservationsPage}" />

</Shell>
